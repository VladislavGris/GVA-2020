number function Comparsion(number a, number b)		; Функция сравнивает 2 числа и возвращает наибольшее
begin
if[a > b] then
{
return a
}
else
{
return b
}
end

number function Rand(number a[])
begin
print(a)
end

number function Equality(number a, number b)		; Функция определяет, равны ли 2 числа
begin
if[a = b] then
{
return 1						; Возвращает 1, если числа равны
}
else
{
return 0						; Возвращает 0, если числа не равны
}
end

main
begin
number a <- 1011b
number b <- 13
symbol c <- 'c'
symbol d
number y[2]
symbol hello[6] <- "Привет"
symbol Hello[6] <- "выходные данные"
number len <- strlen(hello)
number prio <- compare(hello, Hello)
b <- 71o
d <- 'd'
number g <- Comparsion(a, b)
Rand(y)
if[a GE b] then
{
number e <- a
a <- b
b <- e
}

if[a = 5] then
{
print("a = 5")
}
else if[a = 4] then
{
print("a = 4")
}
else
{
print("a не 5 и не 4")
}
if[a # 3] then
{
print("a не равно 3")
}
return 0
end